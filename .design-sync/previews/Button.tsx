import { Button } from 'aegis-app';

export const Primary = () => <Button>Report an incident</Button>;

export const Ghost = () => <Button variant="ghost">Export today's register (CSV)</Button>;

export const Danger = () => <Button variant="danger">Remove contact</Button>;

export const Disabled = () => <Button disabled>Saving…</Button>;
